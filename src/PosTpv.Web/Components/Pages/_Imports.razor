@* Every page in this folder requires an authenticated user unless it opts out with
   [AllowAnonymous]. Individual pages further restrict access with [Authorize(Roles = "...")]. *@
@attribute [Authorize]
