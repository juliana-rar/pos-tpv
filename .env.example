# Copy to .env and adjust. Used by compose.yaml for the SQL Server SA password.
# This is a LOCAL DEV value only — never reuse it (or any value checked into a repo) in production.
SA_PASSWORD=PosTpv!Dev2026
